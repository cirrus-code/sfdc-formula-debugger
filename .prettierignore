# Generated by scripts/extract-corpus.ts, which formats its own output. Letting
# `prettier --write .` reach it rewrites the corpus out of band, which the WS5
# drift job then reports as drift on formatting alone.
corpus/salesforce-v2.json

# Salesforce's formula-engine, cloned on demand by oracle/ci-smoke.sh. Vendored
# upstream source is not ours to reformat.
oracle/formula-engine/
