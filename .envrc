# devenv (flake-parts mode) needs the project root at eval time; passing it
# through the devenv-root file input keeps `use flake` pure-eval (see flake.nix).
mkdir -p "$PWD/.devenv"
printf %s "$PWD" >"$PWD/.devenv/root"
use flake . --override-input devenv-root "file+file://$PWD/.devenv/root"
