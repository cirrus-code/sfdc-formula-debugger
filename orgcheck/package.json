{
  "name": "sfdc-formula-analyzer-orgcheck",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Org-verification harness: probes a real Salesforce dev org to settle VERIFICATION.md open questions. Dev tool only — never part of the app build.",
  "scripts": {
    "generate": "node --experimental-strip-types src/generate.ts",
    "collect": "node --experimental-strip-types src/collect.ts",
    "emit": "node --experimental-strip-types src/emit.ts",
    "sf": "sf"
  },
  "devDependencies": {
    "@salesforce/cli": "^2.100.0",
    "@types/node": "^26.1.1",
    "typescript": "^5.9.3"
  }
}
