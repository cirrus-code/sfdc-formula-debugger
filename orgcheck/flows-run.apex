// Generated by generate-ctx.ts — runs the FxFV_* value-probe flows and
// reports each interview's output variable:
//   CTXRESULT|<probeId>|FLOWVAL|<value>  or  CTXRESULT|<probeId>|FLOWERR|<message>
try {
    Flow.Interview iv = Flow.Interview.createInterview('FxFV_fv_htmlencode', new Map<String, Object>());
    iv.start();
    String v = String.valueOf(iv.getVariableValue('out'));
    // Base64 the payload: the debug channel entity-encodes HTML, which
    // would destroy encode-function outputs (their output IS entities).
    String b64 = EncodingUtil.base64Encode(Blob.valueOf(v == null ? 'null' : v));
    System.debug('CTXRESULT|fv_htmlencode|FLOWVAL64|' + b64);
} catch (Exception e) {
    System.debug('CTXRESULT|fv_htmlencode|FLOWERR|' + e.getMessage().replace('\n', ' '));
}
try {
    Flow.Interview iv = Flow.Interview.createInterview('FxFV_fv_jsencode', new Map<String, Object>());
    iv.start();
    String v = String.valueOf(iv.getVariableValue('out'));
    // Base64 the payload: the debug channel entity-encodes HTML, which
    // would destroy encode-function outputs (their output IS entities).
    String b64 = EncodingUtil.base64Encode(Blob.valueOf(v == null ? 'null' : v));
    System.debug('CTXRESULT|fv_jsencode|FLOWVAL64|' + b64);
} catch (Exception e) {
    System.debug('CTXRESULT|fv_jsencode|FLOWERR|' + e.getMessage().replace('\n', ' '));
}
try {
    Flow.Interview iv = Flow.Interview.createInterview('FxFV_fv_jsinhtmlencode', new Map<String, Object>());
    iv.start();
    String v = String.valueOf(iv.getVariableValue('out'));
    // Base64 the payload: the debug channel entity-encodes HTML, which
    // would destroy encode-function outputs (their output IS entities).
    String b64 = EncodingUtil.base64Encode(Blob.valueOf(v == null ? 'null' : v));
    System.debug('CTXRESULT|fv_jsinhtmlencode|FLOWVAL64|' + b64);
} catch (Exception e) {
    System.debug('CTXRESULT|fv_jsinhtmlencode|FLOWERR|' + e.getMessage().replace('\n', ' '));
}
try {
    Flow.Interview iv = Flow.Interview.createInterview('FxFV_fv_urlencode', new Map<String, Object>());
    iv.start();
    String v = String.valueOf(iv.getVariableValue('out'));
    // Base64 the payload: the debug channel entity-encodes HTML, which
    // would destroy encode-function outputs (their output IS entities).
    String b64 = EncodingUtil.base64Encode(Blob.valueOf(v == null ? 'null' : v));
    System.debug('CTXRESULT|fv_urlencode|FLOWVAL64|' + b64);
} catch (Exception e) {
    System.debug('CTXRESULT|fv_urlencode|FLOWERR|' + e.getMessage().replace('\n', ' '));
}
try {
    Flow.Interview iv = Flow.Interview.createInterview('FxFV_fv_divzero', new Map<String, Object>());
    iv.start();
    String v = String.valueOf(iv.getVariableValue('out'));
    // Base64 the payload: the debug channel entity-encodes HTML, which
    // would destroy encode-function outputs (their output IS entities).
    String b64 = EncodingUtil.base64Encode(Blob.valueOf(v == null ? 'null' : v));
    System.debug('CTXRESULT|fv_divzero|FLOWVAL64|' + b64);
} catch (Exception e) {
    System.debug('CTXRESULT|fv_divzero|FLOWERR|' + e.getMessage().replace('\n', ' '));
}
try {
    Flow.Interview iv = Flow.Interview.createInterview('FxFV_fv_br', new Map<String, Object>());
    iv.start();
    String v = String.valueOf(iv.getVariableValue('out'));
    // Base64 the payload: the debug channel entity-encodes HTML, which
    // would destroy encode-function outputs (their output IS entities).
    String b64 = EncodingUtil.base64Encode(Blob.valueOf(v == null ? 'null' : v));
    System.debug('CTXRESULT|fv_br|FLOWVAL64|' + b64);
} catch (Exception e) {
    System.debug('CTXRESULT|fv_br|FLOWERR|' + e.getMessage().replace('\n', ' '));
}
